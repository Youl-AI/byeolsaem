# 별샘 인수인계 문서

> 작성일 2026-08-09
> 배포 주소: https://byeolsaem.com (구 daily-star-sync.vercel.app → 308 리다이렉트)
> 이 문서는 실제 배포본을 브라우저로 직접 방문·측정한 결과다. 추정치가 아니다.

> **읽기 전에 (2026-08-17 갱신)**
> 이 문서는 **구 사이트(Star Sync)를 진단한 기록**이다. 그 사이트는 리뉴얼로 대체됐고,
> 코드는 저장소에서 삭제됐다(git 히스토리에는 남아 있다). 그러니 §1 실측값과
> §2 "오늘 할 일"은 **당시의 상태**로 읽을 것 — `index.html:374` 같은 행 번호는
> 더 이상 가리킬 파일이 없다.
> 여전히 유효한 것은 **§3 향후 전략 · §4 판단 게이트 · §5 하지 말 것**이다.
> 현재 구조는 `README.md`, 실행 계획은 `RENEWAL_PLAN.md`를 본다.

---

## 0. 한 줄 진단

**제품은 완성됐다. 사람이 오지 않았고, 왔더라도 결과를 못 보고 나갔을 가능성이 크다.
그리고 그 사실조차 확인할 수 없다 — 추적 코드가 없기 때문이다.**

---

## 1. 검증된 사실 (2026-08-09 실측)

브라우저로 직접 방문해 확인한 값이다.

### 측정 불가 상태

| 항목 | 결과 |
| --- | --- |
| Google Analytics (`gtag` / `googletagmanager`) | **없음** |
| 애드센스 스크립트 (`adsbygoogle`) | 있음 — `index.html:38` |
| `ads.txt` | 있음 — `pub-6538739927923803` |

README에는 "GA 연결"이라 적혀 있으나 **배포본에 추적 코드가 없다.**
6개월간 방문자 데이터가 한 건도 쌓이지 않았다.

### SEO

| 항목 | 결과 |
| --- | --- |
| `robots.txt` | **404** |
| `sitemap.xml` | **404** |
| 메인 페이지 `h1` | **0개** (`h2`만 존재) |
| `canonical` | 없음 |
| `hreflang` | 없음 |
| URL 구조 | `blog.html` ~ `blog5.html` — 키워드 없음 |
| 한/영 처리 | **한 페이지에 두 언어 전문이 모두 렌더링** (문서 높이 7,630px) |
| `meta keywords` | "사주" 포함 — 그러나 사이트에 사주 기능·콘텐츠 없음 |

### 콘텐츠 분량

| 항목 | 실측 |
| --- | --- |
| 블로그 1편 본문 | **약 1,100자** (표기는 "읽는 시간 4분") |
| `blog-list.html` 전체 | 686자 |
| 글 편수 | 5편 (`blog.html` ~ `blog5.html`) |
| 사이트 전체 텍스트 | **1만자 미만** |
| 최신 글 작성일 | **2026-03-11** (5개월 방치) |

애드센스 통상 승인권은 **편당 2,000~3,000자 × 20~30편**이다. 현재는 약 1/10 수준이다.
거절 사유가 "가치가 별로 없는 콘텐츠"였다면 원인은 이것이다.

### UX 결함

| 문제 | 위치 / 실측 |
| --- | --- |
| **결과 표시 후 스크롤 없음** | `script.js:553` — `resultArea.style.display = "block"` 직후 스크롤 이동이 없다. 분석 완료 시점에 `scrollY: 540`, `resultArea` 상단은 `1479` — **결과가 뷰포트 바로 아래에서 시작해 사용자가 직접 스크롤하지 않으면 보이지 않는다** |
| 응답 지연 | 약 30초 (Render 무료 티어 콜드 스타트). 로딩 표시는 있음 (`script.js:489`) |
| 문의 링크 깨짐 | `index.html:374` — `mailto:your-email@example.com` 플레이스홀더가 그대로 배포됨 |
| 도시 입력 | `Country` / `City`를 **영어로 직접 타이핑**해야 함 ("South Korea", "Seoul") |
| 모바일 헤더 | "Star Sync", "칼럼 읽기"가 각각 2줄로 접힘 (390px 기준) |
| 천궁도 라벨 겹침 | 라벨 카드가 서로 포개져 글자 판독 불가 — **데스크톱·모바일 모두** |
| `og-image.png` | **7.7MB** — 카카오톡·트위터 미리보기가 실패하거나 매우 느려짐 |
| 쿠팡 제휴 고지 | 페이지 **최상단**에 위치. 심사관이 콘텐츠보다 먼저 봄 |

입력 검증(`script.js:478`)과 로딩 표시는 정상 동작한다.

### 잘 되어 있는 것 (건드리지 말 것)

- **결과물 품질이 높다.** "올해 이직을 해도 될까요?"에 대해 목성·토성 배치를 근거로
  기회/위험을 구분해 답한다. 일반 운세 앱의 "올해는 좋은 일이 있겠습니다" 수준과 다르다.
- 천궁도 시각화 디자인
- 카카오톡 공유 · 결과 이미지 저장 (바이럴 장치)

---

## 2. 지금 할 일 — 우선순위 순

### 🔴 오늘 (합계 약 1~2시간)

- [ ] **Google Analytics 4 설치** — `index.html` `<head>`에 `gtag.js`.
      블로그 5개 페이지에도 동일하게 추가.
      **이것 없이는 이후 어떤 개선도 효과를 측정할 수 없다. 최우선.**
- [ ] **결과 자동 스크롤** — `script.js:553` 다음 줄에 추가:
      ```js
      resultArea.scrollIntoView({ behavior: 'smooth', block: 'start' });
      ```
      30초를 기다린 사용자가 결과를 실제로 보게 만드는 한 줄이다.
- [ ] **`index.html:374` 문의 이메일 수정** — 실제 주소로 교체
- [ ] **메인 페이지 `h1` 추가** — 현재 `h2`만 있음

### 🟠 이번 주

- [ ] `robots.txt` 생성 (`Sitemap:` 지시자 포함)
- [ ] `sitemap.xml` 생성 → **Google Search Console에 제출**
      (Search Console 등록 자체가 아직 안 되어 있다면 그것부터)
- [ ] `og-image.png` 압축 — **7.7MB → 200KB 이하** (1200×630 권장)
- [ ] 도시 입력을 드롭다운/자동완성으로 교체 (한글 지원)
- [ ] 모바일 헤더 깨짐 수정 (`style.css`)
- [ ] 천궁도 라벨 카드 겹침 수정 — 충돌 회피 배치 또는 라벨 수 축소
- [ ] 쿠팡 제휴 고지를 **페이지 하단**으로 이동 (법적 고지는 유지하되 위치만)
- [ ] 한/영 분리: `/ko`, `/en` 경로로 나누고 `hreflang` 추가
      (현재 한 페이지에 두 언어 전문이 모두 들어 있어 중복 콘텐츠로 읽힌다)

### 🟡 애드센스 재신청 전 (약 한 달)

- [ ] 블로그 글을 **편당 2,500~3,000자**로 확대
- [ ] 글 편수 **15편 이상**
- [ ] URL을 한글 키워드 슬러그로 (`blog2.html` → `/12하우스-잠재력` 등)
- [ ] 각 글에 `description`, `h1`, 내부 링크 정비
- [ ] 애드센스 재신청

> **재신청 전 반드시 확인:** 이전 거절 사유 원문을 애드센스 대시보드에서 확인할 것.
> "가치가 별로 없는 콘텐츠"였다면 위 작업으로 해결 가능하다.
> **정책 위반이었다면 원인이 다르므로 위 작업은 헛수고다.** 먼저 확인하고 착수한다.

---

## 3. 향후 전략

### 근본 문제 두 가지

**① 점성술은 한국에서 검색되지 않는다.**
한국 검색 수요는 **사주 · 토정비결 · 궁합 · 신년운세**에 있다. 서양 점성술은 그 옆의 작은 시장이다.
`meta keywords`에 "사주"를 넣어뒀지만 실제 기능이 없어 키워드 불일치 상태다.

> **2026-08-10 대응 결정:** 사주로 확장하는 대신 점성술 안에서 검색 수요를 판다.
> "별자리 궁합", "별자리 운세", "수성 역행", "2027 별자리 운세" 등은 실제 수요가 있다.
> `meta keywords`의 "사주"는 제거할 것 (키워드 불일치 해소).

**② 재방문할 이유가 없다.**
현재 구조는 한 번 보면 끝이다. 운세 서비스의 트래픽은 **매일 여는 사용자**에서 나온다.

| | 포스텔러 · 점신 | 별샘 (2026-08 리뉴얼 전) |
| --- | --- | --- |
| 주제 | 사주 · 토정비결 · 궁합 | 서양 점성술 |
| 콘텐츠량 | 200페이지+ | 1만자 미만 |
| 재방문 장치 | **"오늘의 운세" 매일 갱신** | 없음 |
| 매출원 | 1:1 전화상담 (전문가 400명+) | 쿠팡 링크 1개 |

### 방향

- [x] **사주 기능 — 하지 않기로 결정 (2026-08-10)**
      한국 최대 검색 수요를 포기하는 대신 점성술 하나를 압도적 품질로 판다.
      사주 시장은 전문가 상담까지 붙은 대형사 판이고, 어설픈 사주는 정통성 의심만 산다.
      **대체 전략:** 점성술 기능 다각화 — 오늘의 운세(트랜짓) · 궁합(시너스트리) ·
      연간운세(목성·토성 이동) · 12별자리 페이지 · 수성 역행 캘린더.
      전부 kerykeion이 이미 지원. 상세는 **RENEWAL_PLAN.md** 참조.
- [ ] **"오늘의 운세" 일일 갱신** — 재방문 장치. 이것이 트래픽 누적의 유일한 경로다.
      **실제 트랜짓 기반**(무작위 아님). 12별자리 공통은 새벽 cron 하루 12회 생성 후
      Cloudflare KV 캐시 → 방문자 수와 무관하게 비용 고정
- [ ] **신년 시즌(12월~2월) 대비** — 운세 트래픽이 폭발하는 구간.
      **11월까지 콘텐츠와 SEO가 완성돼 있어야 12월 유입을 받는다. 지금 8월 = 4개월 남음**

### 인프라

- [x] **Vercel Hobby → Cloudflare 이전** — 완료 (2026-08-09).
      byeolsaem.com + www 연결, 구 vercel.app 은 308 리다이렉트 (Vercel 프로젝트 삭제 금지).

### 백엔드 개선 — 9월 리모델링에 포함 (2026-08-10 진단)

**언어 결정: 파이썬 유지.** 핵심 의존성 kerykeion(스위스 천문력 기반 천궁도 계산)이
파이썬 전용이며 결과물 품질의 뿌리다. JS 대체재는 정확도가 떨어진다.
네이티브 C 확장이라 Cloudflare Workers 이전도 불가 — 백엔드는 Render(또는 유사 서버) 유지.
시스템 프롬프트로 페르소나를 주는 현재 방식도 표준이므로 유지한다.

> **2026-08-10 아키텍처 결정 — DB 우선, LLM 옵트인.**
> 광고 수익 모델에서는 **LLM 비용이 트래픽이 아니라 매출에 비례해야 한다.**
> 기본 경로(관심사 선택지 → 해석 아톰 DB 조립)는 **LLM 0회·즉시 응답**으로 가고,
> LLM은 "AI로 더 깊이 보기" 버튼 뒤로 격리한다. 개인화는 차트 계산에서 나오지
> LLM에서 나오는 게 아니다. 상세는 **RENEWAL_PLAN.md §0.2, §2** 참조.
> 이 결정으로 아래 항목들의 우선순위가 바뀌었다.

우선순위 순:

- [x] **연도 하드코딩 수정** — 완료 (2026-08-10).
      `logic.py`에 `get_fortune_year()` 추가: KST 기준 현재 연도를 쓰되
      **11월부터는 다음 해**를 반환한다 (신년운세 수요가 11~12월에 시작되므로
      12월 방문자에게 끝나가는 해의 지표를 주면 안 됨).
      프롬프트 2벌(한/영) + 폴백 키워드 + `main.py` 폴백에 적용.
      `index.html` 메타 태그(description·og·twitter)의 "2026년"도 제거해 상시(evergreen) 문구로 변경.
      정적 HTML은 자체 갱신이 불가하므로 애초에 연도를 넣지 않는 편이 맞다.
      메타 keywords의 "사주"도 제거 (기능 없음 → 키워드 불일치 해소).
- [ ] **차트 데이터 확장 — 품질 최우선.** 현재 AI에 주는 정보가 행성 8개의 별자리·하우스뿐이고
      **어스펙트(행성 간 각도)가 통째로 빠져 있다.** 점성술 해석의 핵심이자
      **아톰 DB 조립의 전제 조건**. kerykeion이 11종 어스펙트를 오브와 함께 이미 계산한다.
      추가: 어스펙트 전체 · 앵글(MC) · 노드 · 역행 플래그 · 달 위상.
- [ ] **결과 캐시** — `hash(입력)` → Cloudflare KV. 현재 `temperature=0.7` + 캐시 없음이라
      **같은 사람이 다시 보면 다른 운세가 나온다** (점술 서비스에서 신뢰 붕괴 요인).
      일관성·비용·속도를 한 번에 해결. **Render 무료 티어는 파일시스템이 휘발성이라
      SQLite를 두면 재시작 시 소멸 — 캐시는 반드시 KV에.**
- [ ] **스트리밍 응답 (SSE)** — **옵트인 LLM 경로에만 적용.**
      기본 경로는 DB 조립이라 즉시 응답이므로 스트리밍이 불필요해졌다 (우선순위 하향).
- [x] ~~**콜드 스타트 제거**~~ — **2026-09-14에 되돌렸다.** 2026-08-10부터 Cloudflare
      Worker `byeolsaem-keepalive`가 10분마다 Render `/`를 핑해 콜드 스타트(실측 131초)를
      없애고 있었다. 그런데 Render `star-sync`가 2026-09-05에 suspend되면서(무료 한도 소진)
      핑이 503만 받아 오게 됐고, 워커를 삭제했다. 이 항목의 원래 조건("Render 백엔드를
      옮기거나 유료 전환하면 이 Worker는 삭제할 것")이 그대로 발동한 셈이다.
      로컬 폴더 `Desktop\Github\byeolsaem-keepalive\`도 함께 지웠다 — git 저장소가
      아니어서 지우면 코드가 사라지므로, 전부를 아래에 옮겨 둔다.
      Render를 되살릴 때 유료 플랜이면 콜드 스타트가 없으므로 이 워커 자체가 불필요하다.
      무료로 되살린다면 빈 폴더에 아래 두 파일을 만들고 `npx wrangler deploy`만 하면 된다.

      `worker.js`:

      ```js
      // Render 무료 티어 콜드 스타트 방지용 keep-alive 핑
      // 10분마다 백엔드를 깨워서 첫 방문자의 ~50초 대기를 없앤다.
      export default {
        async scheduled(event, env, ctx) {
          try {
            const res = await fetch("https://star-sync.onrender.com/", {
              signal: AbortSignal.timeout(60000),
            });
            console.log(`ping ${res.status}`);
          } catch (e) {
            console.log(`ping failed: ${e.message}`);
          }
        },
      };
      ```

      `wrangler.jsonc`:

      ```jsonc
      {
        "name": "byeolsaem-keepalive",
        "main": "worker.js",
        "compatibility_date": "2026-08-01",
        "triggers": {
          "crons": ["*/10 * * * *"]
        }
      }
      ```

      **계정 주의:** 이 워커도 byeolsaem 본체도 Cloudflare 계정 `55fb977e…`에 있다.
      wrangler 자격증명은 전역 파일 하나(`AppData\Roaming\xdg.config\.wrangler\config\default.toml`)라
      다른 프로젝트에서 `wrangler login`을 하면 덮어쓴다. 배포가 `Authentication error
      [code: 10000]`으로 막히면 그 계정으로 다시 로그인하면 된다(2026-09-14에 실제로 겪었다).
- [ ] **지오코딩 로컬화** — 매 요청 Nominatim 외부 호출(초당 1회 제한, 장애 시
      `logic.py:64-68` 이 **조용히 서울 좌표로 잘못된 차트를 계산**하는 버그).
      script.js의 160개 도시 목록에 좌표·타임존을 내장하고 Nominatim은 미지 도시 fallback으로만.
      실패 시 서울 기본값 대신 명시적 에러 반환.
- [ ] **해석 아톰 DB 제작** — 약 2,000개(네이탈 480 + 트랜짓 500 + 관심사 렌즈).
      Gemini로 초안 배치 생성 → 사람 검수 → 저장소에 JSON 커밋.
      **진짜 비용은 API가 아니라 제작 노동(몇 주)이다. 프론트 개발과 병렬 진행 필수.**
      대신 일회성 자산이고, 크롤러가 읽을 수 있어 SEO에 직접 기여한다
      (LLM 생성 결과는 구글이 못 본다 — 콘텐츠 부족이 애드센스 거절 원인이었음).
      ⚠️ 조합마다 페이지 자동 양산 금지 — 구글 scaled content abuse 직격.
      발행은 선별해서 두껍게(12별자리 × 5관심사 = 60페이지, 각 2,000자 이상).
- [ ] **구조화 출력(JSON)** — `### Category:` / `[[아이템]]` regex 파싱은 포맷 이탈 시 깨짐.
      Gemini `response_schema`로 JSON 강제 → `logic.py:215-223` 파싱 코드 삭제.
      **옵트인 LLM 경로에 적용.** AI가 마크다운 레이아웃을 그리지 않게 하고
      점수는 정수로 받아 프론트가 렌더 (문자열 `★★★☆☆`로는 카운트업 연출 불가).
- [ ] **async 블로킹 수정** — `async def analyze` 안에서 동기 호출(Gemini·쿠팡 requests)이
      이벤트 루프를 블로킹. `def`로 바꾸거나(스레드풀) async 클라이언트 사용.
- [ ] **에러 응답 정리** — raw 예외 문자열을 사용자에게 노출 중(`main.py:114,145`).
      내부 로그와 사용자 메시지 분리 + 적절한 HTTP 상태코드.
- [ ] **봇 방어** — Gemini 무료 쿼터 보호. slowapi 등으로 IP당 rate limit.
- [ ] **모델 고정** — 현재 `gemini-3-flash-preview`. preview는 예고 없이 변경될 수 있으므로
      GA 버전 출시 시 고정. (기존 우려였던 2.5 Flash 종료(10/16)는 이미 무관)

---

## 4. 판단 게이트 — 언제 접을지 미리 정해둔다

**2주 뒤(2026-08-23경) GA 데이터를 보고 결정한다.**

| GA 결과 | 해석 | 다음 행동 |
| --- | --- | --- |
| 방문자 거의 0 | 유통 실패. 운세 시장의 문제가 아님 | SEO·콘텐츠 작업 계속. 신년 시즌 목표 |
| 방문자 있음 + 결과 도달률 낮음 | UX 이탈. 자동 스크롤 수정 효과 확인 | 퍼널 개선 계속 |
| 방문자 있음 + 결과 도달률 높음 + 수익 0 | 수익화 실패 | 애드센스 재신청 후 재판단 |

**목표 수준 (냉정한 기준):** 애드센스로 월 10만원을 벌려면 **월 3~10만 PV**가 필요하다.
한국 일반 블로그 Page RPM은 1,000~10,000원 수준이다. 쉽지 않다는 것을 전제로 진행한다.

**접을 조건:** 위 작업을 전부 마치고 신년 시즌(2027년 1~2월)을 통과했는데도
월 PV가 5,000을 넘지 않으면, 이 방향은 접는다.

---

## 5. 하지 말 것

> **2026-08-10 갱신:** 아래 첫 두 항목은 작성 당시(GA 미설치·측정 불가 상태) 기준이었다.
> GA 설치와 Step 1·2가 완료되어 **전면 리뉴얼로 방향이 변경**되었다.
> 대신 리뉴얼 과정에서 지킬 금지 사항을 아래에 새로 둔다.

- ~~**새 기능 추가**~~ → 리뉴얼로 전환. 단 **RENEWAL_PLAN에 없는 기능은 추가하지 않는다**
- ~~**디자인 전면 개편 금지**~~ → 개편하되 **결과물 품질·천궁도 시각화·공유 기능은
  품질 유지하며 이식**한다 (기존 강점, 재작성 아님)
- **GA 없이 다른 작업 먼저 하기** — 효과를 못 재면 개선이 아니라 추측이다
- **애드센스 거절 사유 확인 없이 콘텐츠 늘리기** — 정책 위반이었다면 헛수고다
- **🚨 무료·대량 지면에 LLM 붙이기** — 오늘의 운세처럼 매일 보는 무료 지면에 사용자당
  LLM 호출을 붙이면 **재방문이 늘수록 적자**다. 광고 모델의 생존 조건 (RENEWAL_PLAN §0.2)
- **🚨 DB 조합으로 페이지 자동 양산** — 구글 scaled content abuse 정책 직격.
  애드센스 재신청을 앞두고 있으므로 치명적
- **11월 이후 대규모 변경** — 신년 시즌 진입 전 동결. 색인 리드타임 확보

---

## 6. 저장소 구조 참고

> 2026-08-17 갱신. 아래 구 사이트 트리는 삭제됐고 현재 구조로 대체했다.

```
byeolsaem/
├─ byeolsaem-web/      현재 서비스 (Next 16 · 정적 export → Cloudflare)
├─ main.py             FastAPI 백엔드 (Render). 지금은 호출하는 곳이 없다
├─ logic.py            kerykeion 차트 계산 + Gemini 호출
├─ requirements.txt
├─ measure_latency.py  백엔드 응답 시간 측정
├─ wrangler.jsonc      Cloudflare — byeolsaem-web/out 을 서빙
├─ vercel.json         구 vercel.app → byeolsaem.com 301 (삭제 금지)
├─ HANDOFF.md          이 문서
└─ RENEWAL_PLAN.md     리뉴얼 실행 계획
```

<details>
<summary>삭제된 구 사이트 트리 (2026-08-17 이전)</summary>

```
├─ index.html          메인 (33KB) — 폼 · 결과 영역 · 블로그 카드 · 한/영 동시 포함
├─ script.js           프론트 로직 (36KB) — analyze() 는 468행부터
├─ style.css           (36KB)
├─ blog.html ~ blog5.html   칼럼 5편 (각 약 1,100자)
├─ blog-list.html      칼럼 목록
├─ about.html / privacy.html
├─ ads.txt             pub-6538739927923803  → byeolsaem-web/public/ads.txt 로 이관
└─ og-image.png        ⚠ 7.7MB
```

위 문서에서 참조하는 `script.js:553`, `index.html:374` 같은 행 번호는
전부 이 트리 기준이다. 복원하려면 `git log -- index.html` 로 찾는다.

</details>

---

## 7. 다음 세션 시작 방법

이 폴더에서 세션을 열고 다음과 같이 시작하면 된다.

> `HANDOFF.md` 를 읽고, 2번 "🔴 오늘" 항목 4개를 순서대로 처리해줘.

GA 측정 ID는 미리 발급받아 두면 바로 진행할 수 있다.
