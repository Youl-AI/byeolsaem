# 할 일

정리 2026-09-04. 끝난 항목은 지우지 말고 `[x]`로 두되, 분기가 지나면 접는다.
칼럼 발행 순서는 여기 적지 않는다 — `docs/column-queue.md`가 맡는다.

## 다음에 바로 할 것

- [ ] **재신청 전 자가 점검.** 아무 주소나 다섯 개 골라 열고 "이 페이지만의 것이
      무엇인가"에 답해 본다. 답이 안 나오면 그 페이지가 반려 사유다. 페이지를
      늘리는 것보다 싸고 정확하다.
      **여기서 페이지 늘리기는 멈춘다**(2026-09-05 판단). 글자수는 대리 지표이고,
      실제로 센 수는 셋이었다 — 얇은 것을 색인에서 빼기, 이미 있는 것을 문서에
      보이게 하기, 신뢰 신호(/method) 더하기. 넷째(칼럼 증량)가 가장 약했다.
- [ ] **칼럼 스물일곱 편 눈으로 다시 읽기.** 한 세션에 늘린 글이라 결이 비슷하다.
      어색한 편은 사람이 손대는 것이 맞다.
- [x] **스펙 B — 산재 모션 다섯** (`docs/superpowers/specs/2026-09-06-scattered-motion.md`). 건마다 커밋 하나. — 2026-09-07 구현(브랜치 scattered-motion-result-grammar)
- [x] **스펙 C — synastry·today·yearly에 ResultTabs·ReadingCard 적용.** natal에서 만든 컴포넌트 재사용. 첫 화면은 셋 다 이미 그림이 있어 natal보다 작다. — 2026-09-07 구현(브랜치 scattered-motion-result-grammar)
- [ ] natal 재구성 배포 — main 병합 후 루트에서 `npx wrangler deploy`, 그 뒤 byeolsaem.com/natal에서 같은 실측 반복. 이 브랜치(스펙 B·C)도 같은 배포에 실린다.

## 되돌릴지 정할 것

- [ ] natal 재구성(`git revert 0685106 0f164c0`) — 애드센스 결과와 반응 보고. — 되돌린 뒤 globals.css의 `svg .wheel-core { fill }` 규칙도 손으로 지운다(Task 8 커밋에 있어 두 커밋 revert로는 안 사라진다).
- [ ] **/natal 예시 하늘을 서버로 내린 것 — 애드센스 결과를 보고 판단.**
      `ceebba5` 다음 커밋 하나로 끝나므로 `git revert` 한 번이면 되돌아간다.
      바뀐 것: 저장된 출생 정보를 읽기 전에도 예시 하늘이 그대로 보인다(예전에는
      "하늘을 여는 중입니다"). 자기 차트가 있는 사람은 하이드레이션 뒤에 바뀌므로
      아주 짧게 예시가 스친다.
      판단 기준: 심사를 통과하면 그때 이 연출이 마음에 드는지로 정한다. 반응이
      더 좋으면 그대로 두고, 스치는 것이 거슬리면 되돌린다.

## 잠재워 둔 것

- **Render의 `star-sync` 백엔드 — 2026-09-05 suspend.** 저장소 루트의
  `main.py`·`logic.py`가 그 서비스다(FastAPI + kerykeion + google-genai).
  지금 웹은 이것을 전혀 쓰지 않는다 — 천궁도는 `src/lib/chart.ts`가 브라우저에서
  계산하고 풀이는 미리 써 둔 아톰을 조립한다. 그런데도 배포된 채로 남아 있어
  무료 인스턴스 시간을 먹고 있었고(콜드 스타트 131초로 확인), 무료 한도 소진
  안내가 왔다. 나중에 LLM 해석을 붙일 계획이 있어 지우지 않고 재웠다.
  다시 켤 때 확인할 것: **무료 한도는 이미 소진**되었으므로 유료 플랜이나 다른
  호스트가 필요하다. 그리고 그 서비스에 붙어 있는 `GEMINI_API_KEY`와 쿠팡
  파트너스 키는 오래 방치된 값이라 회전하고 시작한다.
  코드는 그대로 둔다(`RENEWAL_PLAN §6`의 백엔드 계획이 아직 살아 있다).

## 기다리는 것 (날짜가 오면)

- [ ] **장소 그림 두 장 — higgsfield 결제가 되면.** 무료 사용량을 다 써서
      지금은 못 뽑는다(2026-09-05). `PlaceBand`가 까는 그림은 다섯 장뿐인데
      `place-natal.webp` 한 장을 /natal·/chapters·/solar-return 셋이 나눠
      쓰고 있다. 촛불 아래 펼친 천궁도라 /natal에는 맞지만 나머지 둘은 주제가
      다르다.
      - **/chapters** — 한 순간의 하늘이 아니라 태어나 죽을 때까지의 시간이
        주제다. 모래시계·연대기·길게 이어진 회랑 쪽.
      - **/solar-return** — 태양이 태어난 자리로 돌아오는 한 해. 한 바퀴 돌아
        제자리에 오는 장면 쪽.
      제약 셋: 위쪽 1/3은 거의 검게(그 위에 제목이 앉는다) · 밤의 배색(먹빛
      남색 + 금색)에 나머지 다섯 장과 같은 화풍 · 폭 2048px webp, 50KB 안팎.

- [ ] **스레드 토큰 갱신 — 2026-11 초.** 60일 만료. 만료 10일 전부터
      `threads-daily` 워크플로가 Actions에 경고를 띄운다. 갱신 경로:
      developers.facebook.com → byeolsaem-sky-bot → 이용 사례 → Threads API →
      설정 → 사용자 토큰 생성기 → byeolsaem_ → 생성 → `THREADS_ACCESS_TOKEN`
      시크릿 교체. 앱 ID `1055113364036961`.
- [ ] **NAV_NEW 금색 점 정리 — 2026-09-20께.** weekly·calendar·solar-return·
      ephemeris·chapters 다섯 개가 아직 "새로 생긴 페이지" 표시를 달고 있다.
- [ ] **애드센스 재신청 — 2026-10 중순, 칼럼 40편 근처에서.**
      지금 25편. 발행 이력이 끊기지 않는 것이 총량보다 중요하다.

## 사용자 몫

- [ ] Google Search Console·네이버에 `/chapters` 수집 요청
- [ ] DC 점성술 갤러리 글 답글 확인 (호라삿타이 반응 보고 방향 정한다)

## 살펴볼 만한 것 (급하지 않음)

- [ ] **달무리 호흡이 뜰 자리가 없다 — 되돌릴지 옮길지 정할 것.** 스펙 B §5는
      `hero/Moon.tsx`의 달무리를 숨쉬게 했고 그대로 구현했지만(`b0d33dc`),
      그 달을 그리는 `HeroSequence`는 `a423982`에서 수직 세계가 히어로를
      대신하며 홈에서 빠졌다. 지금 저장소 어디에서도 부르지 않는다 —
      `app/(night)/page.tsx` 33행의 주석만 남아 있다. 그래서 이 모션은 어느
      화면에도 나오지 않는다. 셋 중 하나를 고른다: `b0d33dc`를 되돌린다 ·
      지금 홈의 달(그 빛무리는 `VerticalWorld`가 캔버스에 그린다)로 옮긴다 ·
      `HeroSequence`를 다시 세울 계획이 있으면 그대로 둔다. 죽은 컴포넌트 셋
      (`HeroSequence`·`hero/Moon`·`MockChart`)을 지울지도 같이 판단한다.

- [ ] **1987·1988년 서머타임 보정.** 두 해 한국은 서머타임을 시행했다(1987-05-10~10-11,
      1988-05-08~10-09). 지금 엔진은 시간대를 KST 고정(+9)으로 두므로, 이 기간에 태어난
      사람이 기록에 적힌 시각을 그대로 넣으면 한 시간이 어긋난다. 상승궁이 반 자리쯤
      밀리는 크기다. 칼럼(달자리·상승궁·시각 모를 때·사주 비교)에는 "한 시간을 빼라"고
      적어 두었지만, 입력 화면에서 자동으로 잡아 주는 편이 맞다.

- [ ] **첫 방문 폰트 무게.** 첫 화면 전송량 ~680KB 중 폰트가 379KB로 절반이 넘는다.
      마루부리는 이미 서브셋이라 더 줄이기 어렵고, Pretendard Variable이 큰 쪽이다.
      LCP는 640ms로 여유가 크니 지금 손댈 이유는 없다 — 느려지면 그때 본다.
- [ ] **/natal의 CLS 0.069.** 기준(0.1)은 통과하지만 유일하게 0이 아니다.
      원반이 그려지며 아래 글이 밀리는 것으로 보인다. 자리를 미리 잡아 두면 0이 된다.
- [ ] **호라삿타이(태국 점성술).** 넣는다면 계산 축이 하나 더 생긴다 — 항성시라
      아야남사(회귀↔항성 보정, 현재 약 24도) 처리와 태국 요일 체계가 따로 필요하다.
      지금 엔진은 회귀시 전용. DC 반응을 보고 정한다.

- [ ] **한 해 핀 무대 경로의 탭바.** 넓은 화면 + 감소 모드 아님에서는 `YearFlow`의
      무대가 `sticky top-0 h-screen`이라 탭바(`sticky top-16`)와 겹쳐 두지 않았다.
      무대 위 48px을 비우고 탭을 얹을지, 그 화면은 스크롤이 목차라 그대로 둘지 판단.
- [ ] **궁합 카드 → 금실 밝히기의 거리.** 금실이 첫 화면으로 올라가 카드 목록과
      한 화면 넘게 떨어졌다. 커서를 올려도 실이 밝아지는 것이 보이지 않으면
      카드 옆에 작은 금실 축소판을 두거나 연결을 뺀다.

## 끝난 것 (2026-09-05)

- [x] 계산 방법 페이지 `/method` — 천체력·오차·하우스 방식·좌표계를 전부 밝힌다.
      `/about`도 만든 사람과 시작 시기를 담아 보강(`ceebba5`)
- [x] 월별 화면 색인 규칙 — 손으로 쓴 문단이 있는 달만 색인한다. 나머지는
      `noindex`이고 화면에는 그대로 있다. 9·10월 두 달을 씀(`d8c68d4`)
- [x] /natal 예시 하늘을 서버로 — 3,022자 → 3,836자. 되돌릴 커밋은 위 절에(`ac57fb7`)
- [x] 칼럼 스물일곱 편 증량 — 렌더 기준 평균 1,496자 → 2,643자(`556cda1`)
- [x] 주간 화면에 달의 자리 이동 — 사건 없는 주가 사라졌다. 달력과 ics에는
      싣지 않는다(한 달 열두어 개면 삭망이 묻힌다)

## 끝난 것 (2026-09-04)

- [x] 천문력 화면 손질 셋 — "표 읽는 법"을 천궁도 범례와 같은 정의 목록으로,
      달 이름 위에 여백을, 달 이동은 아래 전환 고침으로 함께 풀렸다
- [x] 페이지 전환이 매번 1.2초씩 멈추던 것 — 링크를 가로채 View Transition을
      열고 그 안에서 라우트가 커밋되기를 기다리는 구조라, 그 기다림이 커밋을
      막아 마감 시각까지 버티고 있었다. 실측 1.1~2.1초 → 30ms. 크로스페이드는
      CSS 진입 전환으로 되찾았다(PageFade). 달 이동은 스크롤도 지킨다
- [x] 건너뛰기 링크 — 문서 첫 초점 자리에(`0db1e9f`)

- [x] 솔라 리턴 원반 — natal처럼 왼쪽 원반·오른쪽 범례 두 칸으로(`6079582`),
      좁은 화면에서 원반만 왼쪽에 붙던 것도 함께 고쳤다(`e5fe477`, natal 포함)
- [x] 연간 진행 막대 — 자를 1월·12월로 고정하고 점이 움직이게(`96fea86`)

- [x] RSC 프리페치 404 — 페이지당 13~15건이 전부 사라졌다(`68b9e5a`)
- [x] 접근성 위반 5종 — landmark 이름, 목록 규격, 원반 role, 표 초점, 금색 대비
      (`1160669`, `fa5c90e`)
- [x] 스레드 봇 — 매일 아침 07:30 자동 게시(`5f78a3a`)
